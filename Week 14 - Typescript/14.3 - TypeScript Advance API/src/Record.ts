type Persons = Record<string, number>;

const ages: Persons = {
  Johns: 30,
  Alice: 25,
  Bob: 35
};

// Record<string, number>: Isme string keys hain (jaise "John", "Alice", etc.), aur number unke corresponding values hain (jaise 30, 25, etc.).
// ages object mein har key (name) ka ek number value hai.

